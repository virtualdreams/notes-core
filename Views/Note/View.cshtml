@model notes.Models.NoteViewContainer
@using notes.Helper;

@{
	ViewBag.Title = $"{Model.Note.Title} - ";
}

<div class="row">
	<div class="col-lg-12">
		<div class="page-header">
			<div class="btn-toolbar pull-right">
				<div class="btn-group">
					<button type="button" class="btn btn-default" data-href="@Url.Action("edit", "note", new { id = Model.Note.Title.ToSlug(Model.Note.Id) })"><i class="fa fa-pencil"></i> Edit</button>
					<button class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
	    				<span class="caret"></span>
       				</button>
       				<ul class="dropdown-menu pull-right">
						<li><a href="@Url.RouteUrl("print", new { id = Model.Note.Title.ToSlug(Model.Note.Id) })" target="_blank"><i class="fa fa-print"></i> Print</a></li>
						<li class="divider"></li>
						<li><a href="#" data-toggle="modal" data-target="#trash">
							@if(Model.Note.Trash) {
							<i class="fa fa-fw fa-undo"></i> @:Restore
							} else {
							<i class="fa fa-fw fa-trash-o"></i> @:Delete
							}
							</a>
						</li>
						<li><a href="#" data-toggle="modal" data-target="#info"><i class="fa fa-fw fa-info"></i> Info</a></li>
	  				</ul>
     			</div>
   			</div>
   			<h1>@Model.Note.Title
				<small>@Model.Note.Age</small>
			</h1>
		</div>
	</div>
</div>

<div class="row">
	<div class="col-lg-12">
		<markdown>@Html.Raw(Model.Note.Content)</markdown>
	</div>
</div>
<div class="row tagline">
	<div class="col-lg-12">
		@if(!String.IsNullOrEmpty(Model.Note.Notebook)) {
			<a class="cloud" href="@Url.RouteUrl("notebook", new { id = @Model.Note.Notebook })"><i class="fa fa-book"></i> @Model.Note.Notebook</a>
		}
		@foreach(var tag in Model.Note.Tags) {
			<a class="cloud" href="@Url.RouteUrl("tag", new { id = tag })"><i class="fa fa-tag"></i> @tag</a>
		}
	</div>
</div>

<!-- Modal delete -->
<div class="modal fade" id="trash" tabindex="-1" role="dialog" aria-labelledby="trashLabel" aria-hidden="true" data-id="@Model.Note.Id">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h4 class="modal-title" id="trashLabel">
					@if(Model.Note.Trash) {
					@:Restore note
					} else {
					@:Delete note
					}
				</h4>
			</div>
			<div class="modal-body">
				<div class="row">
					<div class="col-sm-12">
						<div id="error"></div>
					</div>
					<div class="col-sm-12">
					@if(Model.Note.Trash) {
						@:Restore note &quot;@Model.Note.Title&quot;?
					} else {
						@:Delete note &quot;@Model.Note.Title&quot;?
					}
					</div>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
				<button type="button" class="btn btn-primary" id="trashSubmit">Ok</button>
			</div>
		</div>
	</div>
</div>

<!-- Modal information -->
<div class="modal fade" id="info" tabindex="-1" role="dialog" aria-labelledby="infoLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h4 class="modal-title" id="infoLabel">@Model.Note.Title</h4>
			</div>
			<div class="modal-body">
				<div class="row">
					<div class="col-sm-6">
						<b>Created on:</b> @Model.Note.Id.CreationTime.ToLocalTime()
					</div>
					<div class="col-sm-6">
						<b>Version:</b> @Model.Note.Version
					</div>
				</div>
			</div>
			<div class="modal-footer">
				<button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
			</div>
		</div>
	</div>
</div>