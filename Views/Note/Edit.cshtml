@model notes.Models.NoteEditContainer
@using notes.Helper;
@using MongoDB.Bson;

@section title
{
	@if(Model.Note.Id != ObjectId.Empty) {
	@Model.Note.Title
	} else {
	@:Create 
	}
	-
}

<div class="row">
	<div class="col-lg-12">
		<h1 class="page-header">
			@if(Model.Note.Id != ObjectId.Empty) {
			@:Edit note
			} else {
			@:Create note
			}
		</h1>
		<ol class="breadcrumb">
			<li><i class="fa fa-home"></i> <a href="@Url.Action("index", "home")">Home</a></li>
			@if(Model.Note.Id != ObjectId.Empty) {
			<li><i class="fa fa-file"></i> <a href="@Url.Action("view", "note", new { id = Model.Note.Id, slug = Model.Note.Title.ToSlug() })">@Model.Note.Title</a></li>
			}
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-lg-12">
		<form role="form" action="@Url.Action("edit", "note")" method="post" enctype="multipart/form-data" id="note-form" >
			<div class="form-group">
				<label>Title</label>
				<input class="form-control" name="title" value="@Model.Note.Title" />
			</div>
			<div class="form-group">
				<label>Content</label>
				<textarea class="form-control" name="content" rows="20">@Model.Note.Content</textarea>
			</div>
			<input type="hidden" name="id" value="@Model.Note.Id" />
			<button type="submit" class="btn btn-primary">Save</button>
		</form>
	</div>
</div>