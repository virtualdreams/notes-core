@model notes.Models.NoteNotebookContainer
@using notes.Helper

@{
	ViewBag.Title = $"{Model.Notebook} - ";
}

<div class="row">
	<div class="col-lg-12">
		<h1 class="page-header">
			<i class="fa fa-book"></i> @Model.Notebook
		</h1>
	</div>
</div>

<div class="row">
	<div class="col-lg-12">
		
		@await Component.InvokeAsync("Notes", new { model = @Model.Notes })

		<nav>
			<ul class="pager">
				@if(Model.Offset.HasPrevious) {
				<li><a href="@Url.Action("notebook", "note", new { id = Model.Notebook, ofs = Model.Offset.PreviousOffset })">Newer</a></li>
				}
				@if(Model.Offset.HasNext) {
				<li><a href="@Url.Action("notebook", "note", new { id = Model.Notebook, ofs = Model.Offset.NextOffset })">Older</a></li>
				}
			</ul>
		</nav>
			
	</div>
</div>