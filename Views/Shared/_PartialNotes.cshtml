@model IEnumerable<notes.Models.NoteModel>
@using notes.Helper

<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th style="text-align: right; width: 150px;"><i class="fa fs-fw fa-clock-o"></i> Submitted</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var note in Model) {
            <tr>
                <td>
                    <a href="@Url.RouteUrl("view", new { id = note.Id, slug = note.Title.ToSlug() })">@note.Title</a>
                </td>
                <td><span class="pull-right text-muted small"><em>@note.Age</em></span></td>
            </tr>
            }
        </tbody>
    </table>
</div>