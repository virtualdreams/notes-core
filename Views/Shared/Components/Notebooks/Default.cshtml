@model IEnumerable<DistinctAndCountModel>
@using notes.Models;

@foreach(var notebook in Model) {
<a class="dropdown-item" href="@Url.RouteUrl("notebook", new { id = notebook.Name })">@notebook.Name <span class="small text-muted">&times; @notebook.Count</span></a>
}