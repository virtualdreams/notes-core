@model IEnumerable<DistinctAndCountModel>
@using notes.Models;

@foreach(var notebook in Model) {
<a class="dropdown-item" href="@Url.RouteUrl("notebook", new { id = notebook.Id })">@notebook.Id <span class="small text-muted">&times; @notebook.Count</span></a>
}