@model IEnumerable<DistinctAndCountModel>
@using notes.Models;

@foreach(var tag in Model) {
<a class="dropdown-item" href="@Url.RouteUrl("tag", new { id = tag.Id })">@tag.Id <span class="small text-muted">&times; @tag.Count</span></a>
}