@model notes.Models.UserListContainer
@using notes.Helper

@{
	ViewBag.Title = "Accounts - ";
}

<div class="row">
	<div class="col-lg-12">
		<div class="page-header">
			<div class="btn-toolbar pull-right">
				<div class="btn-group">
					<button type="button" class="btn btn-primary" data-href="@Url.Action("create", "account", new { area = "admin" })"><i class="fa fa-user"></i> Create</button>
     			</div>
   			</div>
   			<h1><i class="fa fa-users"></i> Accounts</h1>
		</div>
		<ol class="breadcrumb">
			<li><i class="fa fa-home"></i> <a href="@Url.Action("index", "home", new { area = String.Empty })">Home</a></li>
			<li>Admin</li>
		</ol>
	</div>
</div>

<div class="row">
	<div class="col-lg-12">
		<div class="table-responsive">
			<table class="table">
				<thead>
					<tr>
						<th>Username</th>
						<th class="fit">Role</th>
						<th class="fit">Created</th>
						<th class="fit">Status</th>
					</tr>
				</thead>
				<tbody>
					@foreach(var user in Model.Users) {
					<tr>
						<td><a href="@Url.Action("edit", "account", new { area = "admin", id = @user.Id })">@user.Username</a></td>
						<td class="fit">@user.Role</td>
						<td class="fit">@user.Created</td>
						<td class="fit">@(@user.Enabled ? "Active" : "Disabled" )</td>
					</tr>
					}
				</tbody>
			</table>
		</div>	
	</div>
</div>